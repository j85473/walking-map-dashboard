generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


model Walk {
  id            String   @id
  name          String
  date          DateTime
  distanceMiles Float
  steps         Int
  track         String
  createdAt     DateTime @default(now())
}
